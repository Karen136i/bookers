　<!--編集ページ　-->
　
　<!---[error]フラッシュメッセージ〇-->
<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<!--[error]バリデーション✕-->
<% if @book.errors.any? %> 
  <div id="error_explanation">
    <h2>><%= @book.errors.count %>errors prohibited this book from being saved:</div>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @book do |f| %>

   <h4>Title</h4>
   <%= f.text_field :title %>

   <h4>Body</h4>
   <%= f.text_area :body %>　<!--ここが間違っている？-->

   <%= f.submit 'Update Book' %>
     <%= link_to 'Show',book_path(@book.id) %>
     <%= link_to 'Back',books_path %>

<% end %>

